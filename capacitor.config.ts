import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.srgroveryt.app',
  appName: 'Invitados',
  webDir: 'dist/Firebase-Invitados',
  bundledWebRuntime: false
};

export default config;
